# Discovery notes

Answers to REQUIREMENTS §10 go above this line (BUILD-PLAN Task 0.1).

<!-- extract-workbook:start -->
## Unresolved cells

_Generated by `scripts/extract-workbook.py` on 2026-09-10._

10 cells could not be resolved without an owner answer.

### coating

| cell | field | why |
|---|---|---|
| `S52/T52/U52` | `s_constant/coverage/rate` | constants 5 / 100 / 0.5 are unexplained and column U is mislabelled '# Sides' while acting as a rate; REQUIREMENTS §10 q2 |
| `(none)` | `minimum_charge_usd` | CoatingModel.minimumChargeUsd has no source: W67 holds the loaded quote's own cost (1.0619), not a floor. Left unset until the owner gives a real minimum, alongside REQUIREMENTS §10 q2. |

### laser worksheet

| cell | field | why |
|---|---|---|
| `F33` | `pallet_change_hrs_per_part` | F33 (pallet change) equals F32 (rapid factor) exactly in this save, so the engine's reading -- one pallet change per 100-part batch, 60/100 s per part -- cannot be distinguished from a copy-paste of the cell above it. The reading reproduces §9 and 60/parts_per_sheet does not (4.9% out). Ask the owner: is a pallet change costed once per 100-part run, or once per sheet? REQUIREMENTS §10 q10. |

### materials

| cell | field | why |
|---|---|---|
| `C124` | `thickness_in` | no thickness in name 'NO MATERIAL SELECTED'; workbook has no thickness column |
| `C205` | `thickness_in` | no thickness in name 'CRS GAL HOT DIP 18GA G90 MIN TO 0 SPANGLE'; workbook has no thickness column |
| `C206` | `thickness_in` | no thickness in name 'Laminate'; workbook has no thickness column |

### operations

| cell | field | why |
|---|---|---|
| `F32-51` | `standard_unit` | the workbook records standards as bare numbers with no unit. WELD and GRIND are seeded as `inches` (both carry 200 in column F, and REQUIREMENTS §6 notes 120 in/hr for each -- which of the two the 200 is has to come from the estimator); every other manual row is `pieces`. Confirm with §10 q1. |
| `F32-51` | `kind` | machine-vs-manual is not a column in the workbook; it is asserted by the extractor (MACHINE_OPS = laser, Pega, EM2510NT) and needs an owner confirmation alongside REQUIREMENTS §10 q1. EM2510NT and 30-30 are the uncertain two: EM2510NT is treated as a turret punch, 30-30 as a manual operation on its 250/hr standard. |
| `F32-51` | `standard_per_hr` | dropped as machine-op scaffolding (hours come from the cutting worksheet, not a per-hour standard): LASER (F33 = 100), EM2510NT (60 X 98) (F35 = 7) |

### silkscreen

| cell | field | why |
|---|---|---|
| `F221` | `screen_cost` | SCREEN PRICE CUSTOMER: screen cost blank (customer-supplied screen) |

<!-- extract-workbook:end -->
