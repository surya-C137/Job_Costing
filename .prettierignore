node_modules/
dist/
build/
coverage/

# Generated by scripts/extract-workbook.py - reformatting would create noise
# in diffs that should only ever change when the workbook changes.
packages/db/seed/
packages/calc/test/fixtures/

# Generated by `npm run db:generate` (drizzle-kit). The SQL and the
# snapshot it checks against have to stay byte-for-byte what the tool
# wrote, or the next `generate` sees a diff that is not a schema change.
packages/db/drizzle/

# The workbook's own reference material.
docs/reference/
