/**
 * App shell placeholder. The real shell — login, left nav (Quotes, Customers,
 * Settings), toasts, error boundary, and the `/dev/kitchen-sink` route — is
 * BUILD-PLAN Task 4.1.
 */
export function App() {
  return (
    <main className="shell">
      <h1>ShopQuote</h1>
      <p className="muted">
        Scaffold only. The estimator arrives in Phase 4; the costing engine has to reproduce the
        workbook first.
      </p>
    </main>
  );
}
